<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alessandro.androcard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cadastrar1Activity" />
        <activity android:name=".Cadastrar2Activity" />
        <activity android:name=".Dados_PessoaActivity" />
        <activity android:name=".TelaInicialActivity" />
        <activity android:name=".EuActivity" />
        <activity android:name=".DadosFisicosActivity" />
        <activity android:name=".HistoricoActivity"></activity>
    </application>

</manifest>